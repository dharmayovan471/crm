_format_version: "3.0"
_transform: true

services:
  - name: crm-backend-service
    url: http://backend:3000
    routes:
      - name: crm-backend-routes
        paths:
          - /api/v1
        strip_path: false
plugins:
  - name: rate-limiting
    config:
      minute: 60
      policy: local
